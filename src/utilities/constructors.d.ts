export declare class Note {
    constructor(id: number, title: string, message: string, creationDate: Date, order: number);
}

export declare class Change {
    constructor(actions: {}, changes: {})
}